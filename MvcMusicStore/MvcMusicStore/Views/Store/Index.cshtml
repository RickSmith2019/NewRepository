@model IEnumerable<MvcMusicStore.Models.Album>
@{
    ViewBag.Title = "Index";
}

<h2>Here are the list of albums</h2>

<ul>


    @foreach (var item in Model)
    {
        <li>

            @Html.ActionLink(item.Title, "Details", new { id = item.AlbumId })

        </li>
    }

</ul>
@Html.ActionLink("Back to Browse", "Browse")
